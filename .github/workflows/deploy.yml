name: S-STUDY CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python 3.11
      uses: actions/setup-python@v3
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r backend/requirements.txt
        pip install pytest httpx

    # In a real scenario, we would set up a test database service here
    # - name: Run Tests
    #   run: |
    #     cd backend
    #     pytest

    - name: Build Docker Image
      run: |
        docker build -t s-study-backend ./backend

    # Deployment step would go here, e.g., pushing to Render or a registry
    # - name: Deploy to Render
    #   if: github.ref == 'refs/heads/main'
    #   env:
    #     deploy_url: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
    #   run: |
    #     curl "$deploy_url"
